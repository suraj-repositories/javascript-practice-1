/* SOME USEFUL MATH MEHODS 
math has lots of mthods*/

let x = 3.14 ;
let y = 5;
let z =9;
let maximum;
let minimum;

// x = Math.round(x);      // round the variable -- remove decimal number
// x = Math.floor(x);      // rounde but with  short limit
// x = Math.ceil(x);       // round but with higher limit
// x = Math.pow(x, 2);     // to use power in js
// x = Math.sqrt(x);       // find squre root
// x = Math.abs(x);        // it returns the absolute value of the number either number is positive or negative
// X= Math.PI;              // pi value returned

console.log(x);

// maximum = Math.max(x, y, z);       // returns max value
// minimum = Math.min(x, y, z);        // returns min value

// console.log(maximum);
// console.log(minimum);